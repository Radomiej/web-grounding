# Ćwiczenie 03 — PHP w HTML

Plik `index.php` zaczyna się od przygotowania danych, a później przechodzi do HTML. Zmień imię, dodaj temat do tablicy i sprawdź różnicę między `echo` a skrótem `<?= ... ?>`.

