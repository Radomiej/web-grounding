# Ćwiczenie 05 — formularz i SELECT

Formularz wysyła `country` metodą `POST` do tego samego pliku. PHP sprawdza dozwoloną wartość, przekazuje ją do przygotowanego zapytania i generuje listę wyników. Zwróć uwagę, że wartość nie jest doklejana do tekstu SQL.

